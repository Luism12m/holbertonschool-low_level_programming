#include <stdio.h>

/**
 * main - entry poit of the program
 * 
 * Return: Always 0 (Success) 
*/
int main(void)
{
  for(int i = 0; i < 5; i++)
    {
      printf("Numero : %d\n",i);
    }
  return (0);
}
